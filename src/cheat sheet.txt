epaper
======

(ps : ai generated cheat sheet (i was too lazy))

# MÉMO ÉCRAN 2.9" E-PAPER (B) V4 - PICO 2 W

## CÂ²BLAGE
┌──────────────┬────────────┐
│ Écran        │ Pico 2 W   │
├──────────────┼────────────┤
│ VCC          │ 3.3V       │
│ GND          │ GND        │
│ DIN (MOSI)   │ GP11       │
│ CLK (SCK)    │ GP10       │
│ CS           │ GP9        │
│ DC           │ GP8        │
│ RST          │ GP12       │
│ BUSY         │ GP13       │
└──────────────┴────────────┘

## INITIALISATION
```python
import Pico_ePaper_2_13_B_V4 as epd
epd = epd.EPD_2in9_B_V4_Portrait()
```

## FONCTIONS PRINCIPALES

### Effacer l'éµ²cran
```python
epd.Clear()  # Full refresh (10-15s)
```

### Dessiner
```python
# Buffer noir (noir/blanc)
epd.imageblack.text("Texte", x, y, 0x00)  # 0x00=black, 0xFF=white
epd.imageblack.rect(x, y, w, h, 0x00)
epd.imageblack.fill_rect(x, y, w, h, 0x00)
epd.imageblack.line(x1, y1, x2, y2, 0x00)
epd.imageblack.circle(x, y, r, 0x00)

# Buffer rouge (rouge/blanc)
epd.imagered.text("Texte", x, y, 0xFF)  # 0xFF=white, 0x00=red
epd.imagered.fill(0x00)  # Remplir en rouge
epd.imagered.fill(0xFF)  # Remplir en blanc
```

### Afficher
```python
epd.display()       # Full refresh (10-15s) - propre
epd.display_Fast()  # Fast refresh (4-6s) - ghosting
# epd.display_Partial(buf, x1, y1, x2, y2)  # Ne marche pas sur 3 couleurs
```

### Mettre en veille
```python
epd.sleep()  # À appeler à la fin
```

## EXEMPLE COMPLET
```python
import Pico_ePaper_2_13_B_V4 as epd
import utime

epd = epd.EPD_2in9_B_V4_Portrait()
epd.Clear()

epd.imageblack.text("Hello", 10, 10, 0x00)
epd.imagered.text("World", 10, 30, 0xFF)
epd.display()

utime.sleep(5)
epd.sleep()
```

## NOTES
- Full refresh : 10-15s (normal pour 3 couleurs)
- Partial refresh : ne marche pas sur écran 3 couleurs (B)
- Faire un Clear() toutes les 10 updates pour éviter ghosting
- SPI : 4-8 MHz (déµ²jàµ² optimiséµ²)
